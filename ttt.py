Суть задачи:
Необходимо внедрить «безопасный слой» редактирования магазина в админ-панели, чтобы администратор вносил изменения не напрямую в основные таблицы базы данных (Product, Category), а в их временные копии (TempProduct, TempCategory).

Краткий план реализации:

Изоляция: При входе в админ-редактор данные копируются из «основы» во «временные» таблицы (sync_to_temp).

Работа в Temp: Все действия администратора (добавление, удаление, изменение товаров/категорий) теперь выполняются исключительно с использованием Temp-моделей.

Фиксация изменений: По нажатию кнопки «Сохранить изменения» вызывается метод commit_changes, который очищает «основные» таблицы и переносит в них актуальные данные из «временных» (финализация транзакции).

Это позволит избежать ситуации, когда «битые» или промежуточные данные попадают к обычным пользователям до того, как администратор нажал кнопку сохранения.